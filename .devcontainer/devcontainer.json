{
  "name": "kokic/on-site-install",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y fonts-noto-core fonts-noto-cjk fonts-noto-color-emoji && fc-cache -fv",
  "customizations": {
    "vscode": {
      "extensions": [
        "myriad-dreamin.tinymist"
      ]
    }
  }
}
